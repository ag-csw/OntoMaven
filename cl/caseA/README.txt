Case A. A simple import.

We assume that all files at the top level (directly under /caseA) are considered part of the corpus, except this README.

One possible logically-equivalent rewriting, where importations have been removed and xi:include is used instead, appears in the subdirectory /result.
